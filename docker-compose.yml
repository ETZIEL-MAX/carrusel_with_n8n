services:
  carrusel:
    build: .
    container_name: carrusel
    ports:
      - "8080:8080"
    environment:
      LOCAL_STORAGE: "1"
      PORT: "8080"
      ADMIN_PASSWORD: "10Cuidado.2026"
      JWT_SECRET: "dev-jwt-secret-0123456789abcdef"
      WEBHOOK_SECRET: "dev-webhook-secret"
    volumes:
      - ./.data:/app/.data
    restart: unless-stopped
